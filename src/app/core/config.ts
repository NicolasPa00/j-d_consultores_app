/**
 * Configuración del cliente HTTP. Apunta al backend sst_ws.
 * Para producción, cambiar API_BASE (o inyectar por variable de entorno del build).
 */
export const API_BASE = 'http://localhost:4000/api';
